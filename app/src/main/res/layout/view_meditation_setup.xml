<?xml version="1.0" encoding="utf-8"?>


    <!-- SETUP-BEREICH: vor der Meditation -->
    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/layout_setup"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:showIn="@layout/activity_meditation">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_gravity="center"
            android:paddingBottom="8dp"
            android:gravity="center_vertical">

            <ImageButton
                android:id="@+id/button_less"
                android:layout_width="100dp"
                android:layout_height="90dp"
                android:scaleType="fitCenter"
                android:contentDescription="less"
                android:src="@drawable/ic_less"
                android:background="@android:color/transparent"
                android:layout_gravity="center"/>

            <TextView
                android:id="@+id/text_timer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="00"
                android:textColor="@android:color/black"
                android:textSize="48sp"
                android:textStyle="bold"

                />

            <ImageButton
                android:id="@+id/button_more"
                android:layout_width="100dp"
                android:layout_height="90dp"
                android:padding="8dp"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_more"
                android:background="@android:color/transparent"
                android:layout_gravity="center"
                />
        </LinearLayout>
        <!-- FIXED DurationChooser block (same height in both modes) -->
        <LinearLayout
            android:id="@+id/layout_duration_chooser"
            android:layout_width="match_parent"
            android:layout_height="150dp"
            android:orientation="vertical"
            android:layout_marginTop="0dp"
            android:layout_marginBottom="0dp">


            <!-- Content area stays constant -->
            <FrameLayout
                android:id="@+id/frame_duration_content"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1">

                <!-- MANUAL: your quick buttons -->
                <androidx.constraintlayout.widget.ConstraintLayout
                    android:id="@+id/layout_manual"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent">

                    <!-- Optionen -->
                    <GridLayout
                        android:id="@+id/layout_duration_buttons"
                        android:layout_width="0dp"
                        android:layout_height="0dp"
                        android:columnCount="3"
                        android:rowCount="2"
                        android:useDefaultMargins="true"
                        app:layout_constraintTop_toTopOf="parent"
                        app:layout_constraintStart_toStartOf="parent"
                        app:layout_constraintEnd_toEndOf="parent"
                        app:layout_constraintBottom_toTopOf="@id/button_duration_toggle"/>

                    <!-- Toggle → Suggestion -->
                    <Button
                        android:id="@+id/button_duration_toggle"
                        style="@style/TextLinkButton"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:minHeight="40dp"
                        android:text="@string/meditation_mode_suggestion"
                        android:layout_marginEnd="8dp"
                        android:layout_marginBottom="4dp"
                        app:layout_constraintBottom_toBottomOf="parent"
                        android:background="@android:color/transparent"


                        app:layout_constraintEnd_toEndOf="parent"/>

                </androidx.constraintlayout.widget.ConstraintLayout>


                <!-- SUGGESTION: history insight sentence -->
                <androidx.constraintlayout.widget.ConstraintLayout
                    android:id="@+id/layout_suggestion"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:visibility="gone">

                    <TextView
                        android:id="@+id/tv_suggestion_text"
                        style="@style/NormalText"
                        android:layout_width="0dp"
                        android:layout_height="0dp"
                        android:gravity="center"
                        android:text="@string/history_not_yet"
                        app:layout_constraintTop_toTopOf="parent"
                        app:layout_constraintStart_toStartOf="parent"
                        app:layout_constraintEnd_toEndOf="parent"
                        app:layout_constraintBottom_toTopOf="@id/button_suggestion_toggle"
                      />

                    <Button
                        android:id="@+id/button_suggestion_toggle"
                        style="@style/TextLinkButton"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:minHeight="40dp"
                        android:text="@string/meditation_mode_manual"
                        android:paddingStart="12dp"
                        android:paddingEnd="12dp"
                        android:layout_marginEnd="8dp"
                        android:layout_marginBottom="4dp"
                        android:background="@android:color/transparent"

                        app:layout_constraintBottom_toBottomOf="parent"
                        app:layout_constraintEnd_toEndOf="parent"/>




                </androidx.constraintlayout.widget.ConstraintLayout>



            </FrameLayout>
        </LinearLayout>


        <LinearLayout
            android:id="@+id/layout_audio_option"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                style="@style/NormalText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/audio_abspielen" />

            <androidx.appcompat.widget.SwitchCompat
                android:id="@+id/switch_audio"
                style="@style/BF.EinkSwitch"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>

        <Space
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />
    <!-- Bottom area: stays together -->
    <LinearLayout
        android:id="@+id/layout_bottom_area"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <Button
            android:id="@+id/button_start_meditation"
            style="@style/ActionButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/start" />

        <include layout="@layout/view_footerbuttons" />
    </LinearLayout>

 </LinearLayout>